include "filterset_rule.fbs";

namespace circonus;

table Filterset {
  name: string (id: 0);
  seq: long (id: 1);
  cull: bool (id: 2);
  rules:[FiltersetRule] (id: 3);
}

root_type Filterset;
file_identifier "CIMM";
